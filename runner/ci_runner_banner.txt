============================================================
   GitHub Actions 自架 runner
============================================================

   這個視窗就是 CI。關掉它，CI 就停擺。
   看門狗會在 10 分鐘內救回來，但那一刻正在跑的建置會失敗。

   畫面停著不動是正常的 —— runner 的輸出全部寫進日誌：
     C:\actions-runner\_diag\run-detached.log

   看門狗每 10 分鐘記一行：
     C:\actions-runner\_diag\watchdog.log

   視窗不見了 = runner 死了。這盞燈只有這一個意思。
============================================================
